<Window x:Class="DeliveryNoteGenerator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DeliveryNoteGenerator"
        mc:Ignorable="d"
        Title="Interactive Workders delivery note generator" Height="500" Width="800">
    <StackPanel HorizontalAlignment="Center">
        <TextBlock Text="Delivery note Generator" FontSize="40" Margin="0 0 0 30"/>
        <Grid >
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>

            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            
            <Label Grid.Row="0">Responsible:</Label>
            <ComboBox
                Grid.Row="1"
                Grid.Column="0"
                Grid.ColumnSpan="2"
                x:Name="ResponsibleSelector"
                IsEditable="True"
                IsTextSearchEnabled="False"
                StaysOpenOnEdit="True"
                ItemsSource="{Binding FilteredUsersList, UpdateSourceTrigger=PropertyChanged}"
                DisplayMemberPath="name"
                Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                SelectedItem="{Binding SelectedUser, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            
            <Label Grid.Row="0" Grid.Column="3">Date: </Label>
            <DatePicker Grid.Row="1" Grid.Column="3" Grid.ColumnSpan="2"></DatePicker>
        </Grid>
        
        
        <Button Margin="50" Width="60" Click="OpenAddItemModal">Add Item</Button>
        
        <Button Margin="50" Width="60">Issue</Button>
    </StackPanel>
</Window>
